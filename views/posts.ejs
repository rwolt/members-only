<!DOCTYPE html>
<html>
  <head>
    <title><%= title ? title : ''%></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <% if (currentUser && currentUser.membershipStatus === 'guest') { %> <%-
    include('./partials/banner') %> <% } %>
  </body>
</html>
